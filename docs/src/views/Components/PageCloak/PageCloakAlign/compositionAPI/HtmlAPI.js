export default function HtmlAPI() {
  const codeHtml = `<a-cloak
  align="left"
></a-cloak>
<a-cloak
  align="center"
></a-cloak>
<a-cloak
  align="right"
></a-cloak>`;

  return {
    codeHtml,
  };
}
