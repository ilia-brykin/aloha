export default function JsAPI() {
  const codeJs = `import { 
  ASpinner,
} from "aloha-vue";
    
export default {
  name: "PageSpinnerColors",
  components: {
    ASpinner,
  },
};`;

  return {
    codeJs,
  };
}
