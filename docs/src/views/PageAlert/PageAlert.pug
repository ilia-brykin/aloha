div
  a-alert(
    v-for="alert in alerts"
    :key="alert"
    :type="alert"
    :is-visible="!isAlertsHidden[alert]"
    :is-dismissible="true"
    @on-dismiss="closeAlert(alert)"
  )
    div Alert {{ alert }}