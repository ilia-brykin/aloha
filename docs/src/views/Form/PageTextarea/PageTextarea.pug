aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_A_TEXTAREA_COMPONENT_DESCRIPTION_"
    )

    page-textarea-basic

    page-textarea-change

    page-textarea-help-text

    page-textarea-errors

    page-textarea-label-float

    page-textarea-is-scalable

    page-textarea-resize

    aloha-table-props(
      :data="dataProps"
    )

    aloha-table-props(
      table-label="Events"
      :data="dataEvents"
      :columns="['name', 'type', 'description']"
    )
