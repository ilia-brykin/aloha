aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_PAGE_FILTER_FILE_SIZE_DESCRIPTION_"
    )

    page-filter-import-function(
      function-name="filterFileSize"
      type-import="filters"
    )

    page-filter-import-composition-api(
      function-name="filterFileSize"
    )

    page-filter-arguments(
      :arguments-text="argumentsText"
      function-description="filterFileSize(value, { [units=\"kb\"], [sourceUnits=\"b\"], [digits=2] })"
    )

    page-filter-file-size-example

    page-filter-file-size-units

    page-filter-file-size-units-empty

    page-filter-file-size-source-units

    page-filter-file-size-digits

    aloha-table-translate(
      :data="dataTranslate"
    )

    page-filter-test(
      :test="test"
    )
