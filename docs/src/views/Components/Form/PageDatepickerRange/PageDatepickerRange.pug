aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_A_DATEPICKER_RANGE_DESCRIPTION_"
    )

    page-datepicker-range-basic

    page-datepicker-range-custom-first-day

    page-datepicker-range-disabled-variants

    page-datepicker-range-format-customization

    page-datepicker-range-help-text

    page-datepicker-range-readonly

    aloha-table-props(
      :data="dataProps"
    )

    aloha-table-translate(
      :data="dataTranslate"
    )
