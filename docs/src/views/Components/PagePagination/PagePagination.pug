aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_A_PAGINATION_COMPONENT_DESCRIPTION_"
    )

    page-pagination-basic
    page-pagination-limits-per-page
    page-pagination-max-pages
    page-pagination-disabled

    aloha-table-props(
      :data="dataProps"
    )

    aloha-table-translate(
      :data="dataTranslate"
    )
