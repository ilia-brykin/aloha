aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_PAGE_FILTER_MASK_DESCRIPTION_"
    )

    page-filter-import-function(
      function-name="filterMask"
      type-import="filters"
    )

    page-filter-import-composition-api(
      function-name="filterMask"
    )

    page-filter-arguments(
      :arguments-text="argumentsText"
      function-description="filterMask(value, { [chars=4], [mask=\"*\"] })"
    )

    page-filter-mask-example

    page-filter-test(
      :test="test"
    )
