nav.a_d_flex.a_justify_content_between.a_align_items_center.a_px_2(
  style="height: 80px;"
)
  a-menu-button-toggle

  router-link.a_d_flex.a_align_items_center(
    to="/"
  )
    a-icon(
      aria-label="Startseite"
      icon="Aloha"
      width="50"
      height="50"
    )
    span.h1.mb-0.ms-4.align-bottom ALOHA

  form.a_d_flex
    select.a_mr_2(
      v-model="modelFramework"
      @input="changeFramework"
    )
      option(
        v-for="framework in frameworks"
        :key="framework"
        :value="framework"
      ) {{ framework }}

    select(
      v-model="modelLanguage"
      @input="changeLanguage"
    )
      option(
        v-for="language in languages"
        :key="language.value"
        :value="language.value"
      ) {{ language.label }}
