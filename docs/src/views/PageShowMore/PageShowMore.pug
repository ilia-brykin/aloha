aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_A_SHOW_MORE_COMPONENT_DESCRIPTION_"
    )

    page-show-more-basic

    page-show-more-disabled

    page-show-more-html

    page-show-more-not-less

    page-show-more-btn-class

    page-show-more-btn-icons

    aloha-table-props(
      :data="dataProps"
    )

    aloha-table-props(
      table-label="Slots"
      :data="dataSlots"
      :columns="['name', 'description']"
    )

    aloha-table-props(
      table-label="Events"
      :data="dataEvents"
      :columns="['name', 'type', 'description']"
    )
