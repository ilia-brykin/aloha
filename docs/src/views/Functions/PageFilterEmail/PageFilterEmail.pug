aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_PAGE_FILTER_EMAIL_DESCRIPTION_"
    )

    page-filter-import-function(
      function-name="filterEmail"
    )

    page-filter-import-composition-api(
      function-name="filterEmail"
    )

    page-filter-arguments(
      function-description="filterEmail(email, { [linkClass] })"
      placeholder="_PAGE_FILTER_EMAIL_ARGUMENTS_"
    )

    page-filter-email-example

    page-filter-email-link-class

