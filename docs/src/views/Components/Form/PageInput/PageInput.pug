aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_A_INPUT_COMPONENT_DESCRIPTION_"
    )

    page-input-basic

    page-input-change

    page-input-help-text

    page-input-errors

    page-input-label-screen-reader

    page-input-label-float

    page-input-password

    page-input-readonly

    page-input-readonly-password

    aloha-table-props(
      :data="dataProps"
    )

    aloha-table-props(
      table-label="Events"
      :data="dataEvents"
      :columns="['name', 'type', 'description']"
    )

    aloha-table-translate(
      :data="dataTranslate"
    )
