aloha-example(
  :code-html="codeHtml"
  :code-js="codeJs"
  header="Retrieve invalid entries"
  description="Loads ordered multiselect options from local JSON files and renders missing selected values inside a separate invalid entries group."
  :props="['url', 'url-retrieve']"
)
  a-multiselect-ordered(
    v-model="model1"
    key-id="id"
    key-label="label"
    label="Ordered list with retrieve"
    select-label="Add value"
    :url="url"
    :url-retrieve="urlRetrieve"
  )
  div model1: {{ model1 }}
