div
  h1 AModal
  button(
    type="button"
    @click="openModal"
  ) Open Modal

  a-modal(
    v-if="isModalOpen"
    size="xxl"
    :close="closeModal"
  )
    template(
      v-slot:modalBodyPrepend
    )
      button.a_btn.a_btn_secondary(
        type="button"
        @click="openConfirmLocal"
      ) Open confirm