aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_PAGE_FILTER_LIMIT_TO_DESCRIPTION_"
    )

    page-filter-import-function(
      function-name="filterLimitTo"
    )

    page-filter-import-composition-api(
      function-name="filterLimitTo"
    )

    page-filter-arguments(
      :arguments-text="argumentsText"
      function-description="filterLimitTo(text, { [limit=30], [maxThreeDots=true] })"
    )

    page-filter-limit-to-limit

    page-filter-limit-to-max-three-dots

