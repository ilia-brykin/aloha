aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_PAGE_FILTER_LINK_DESCRIPTION_"
    )

    page-filter-import-function(
      function-name="filterLink"
    )

    page-filter-import-composition-api(
      function-name="filterLink"
    )

    page-filter-arguments(
      :arguments-text="argumentsText"
      function-description="filterLink(url, { [linkText=\"\"], [protocol=\"https://\"], [target=\"\"], [linkClass=\"\"] })"
    )

    page-filter-link-example

    page-filter-link-link-text

    page-filter-link-protocol

    page-filter-link-target

    page-filter-link-link-class

    page-filter-test(
      :test="test"
    )
