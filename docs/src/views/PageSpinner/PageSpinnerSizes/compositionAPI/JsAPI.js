export default function JsAPI() {
  const codeJs = `import { 
  ASpinner,
} from "aloha-vue";
    
export default {
  name: "PageSpinnerSizes",
  components: {
    ASpinner,
  },
};`;

  return {
    codeJs,
  };
}
