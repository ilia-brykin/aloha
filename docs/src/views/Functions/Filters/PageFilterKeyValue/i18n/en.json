{
  "_PAGE_FILTER_KEY_VALUE_ARGUMENTS_VALUE_": "The JavaScript object whose key-value pairs are to be converted into a formatted string",
  "_PAGE_FILTER_KEY_VALUE_DESCRIPTION_": "The function <strong>filterKeyValue</strong> creates a formatted string from the key-value pairs of a JavaScript object. It uses the reduce function to iterate through the pairs and convert them into a readable format."
}
