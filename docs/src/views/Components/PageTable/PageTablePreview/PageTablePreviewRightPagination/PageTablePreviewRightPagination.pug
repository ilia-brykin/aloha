aloha-example(
  :code-html="codeHtml"
  :code-js="codeJs"
  header="_A_TABLE_GROUP_PREVIEW_RIGHT_PAGINATION_HEADER_"
  description="_A_TABLE_GROUP_PREVIEW_RIGHT_PAGINATION_DESCRIPTION_"
  :props="['preview', 'pagination.use']"
)
  div
    a-table(
      :columns="columns"
      :data="data"
      label="_A_TABLE_GROUP_SIMPLE_LABEL_"
      key-id="id"
      preview="right"
      :pagination="{ use: true }"
    )
      template(
        v-slot:preview="vm"
      )
        pre {{ vm }}
