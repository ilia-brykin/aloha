aloha-page(
  :page-title="pageTitle"
)
  template(
    v-slot:body
  )
    a-translation(
      tag="p"
      html="_A_SELECT_STYLE_COMPONENT_DESCRIPTION_"
    )

    page-select-style-basic

    page-select-style-change

    page-select-style-label-description

    page-select-style-filter-groups

    page-select-style-data-extended

    page-select-style-readonly

    aloha-table-props(
      :data="dataProps"
    )

    aloha-table-props(
      table-label="Events"
      :data="dataEvents"
      :columns="['name', 'type', 'description']"
    )

    aloha-table-translate(
      :data="dataTranslate"
    )
